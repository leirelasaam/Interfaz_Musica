<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Interfaz_Musica_XML" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Interfaz_Musica_XML" parent="Base.Theme.Interfaz_Musica_XML" />

    <style name="CustomSeekBar" parent="Widget.AppCompat.SeekBar">
        <!-- Color de la parte del progreso de la barra -->
        <item name="android:progressTint">@color/purple</item>
        <!-- Color del fondo de la barra (parte no recorrida) -->
        <item name="android:progressBackgroundTint">@color/dark_purple</item>
        <!-- Color del control deslizante (thumb) -->
        <item name="android:thumbTint">@color/white</item>
    </style>
</resources>